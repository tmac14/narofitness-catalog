# Decision Log

Durable record of approved and pending decisions that affect task planning,
scope, architecture, parallel execution, or validation.

## Decision States

- `PENDING`
- `DECIDED`
- `SUPERSEDED`
- `REVOKED`

## Decisions

| ID | State | Date | Decision | Rationale / Consequence |
|---|---|---|---|---|
| CTRL-D001 | DECIDED | 2026-06-12 | Every change-capable task selects exactly one protocol: `ORCHESTRATION` or `CODEX_IMPLEMENTATION`. | Prevents mixed ownership and ambiguous execution responsibility. |
| CTRL-D002 | DECIDED | 2026-06-13 | Every implementation requires rigorous discovery and an approved plan before edits. | Implementation cannot start before the universal planning gate. |
| CTRL-D003 | DECIDED | 2026-06-13 | `TASK_REGISTRY.yaml` is authoritative for task status, dependencies, write scopes, locks, and parallel safety. | Historical coordination snapshots cannot reactivate stale work or override live control. |
| CTRL-D004 | DECIDED | 2026-06-13 | Parallel execution is deny-by-default until dependency, lock, write-scope, contract, and validation checks pass. | Prevents file collisions and cross-task baseline contamination. |
| CTRL-D005 | DECIDED | 2026-06-13 | Separate executor and validator whenever practical. | Reduces self-confirming validation and strengthens closure evidence. |
| CTRL-D006 | DECIDED | 2026-06-13 | Active task context is persisted in task packets; chat history is not an authoritative task record. | Enables rich recovery after context loss. |
| CTRL-D007 | DECIDED | 2026-06-13 | Codex may maintain only the control documents listed in `AGENTS.md` after material transitions without separate documentation permission. | Keeps recovery data current while preventing silent roadmap, priority, or product changes. |
| CTRL-D008 | DECIDED | 2026-06-13 | The project-control modernization program runs as sequential gated PRs; the next PR cannot start until the previous PR is validated and closed. | Prevents migration, cleanup, tooling, and remediation phases from contaminating each other's baseline. |
| CTRL-D009 | DECIDED | 2026-06-13 | `Runtime` + `Protocol` is the canonical per-task/session selection; supersedes protocol-only selection from CTRL-D001 for new sessions. CTRL-D001 remains the historical record of one protocol per task. | Aligns decisions with the post-RUNTIME-SYMMETRY matrix and Cursor workspace rules. |
| DOC-D001 | DECIDED | 2026-06-13 | English is canonical for technical documentation, code, tests, task control, and tooling; Spanish remains for product UI/user-facing content and source evidence. | Establishes a stable language boundary without unsafe bulk translation. |
| DOC-D002 | DECIDED | 2026-06-13 | Approve PR-04 removal of the five fully consolidated legacy coordination sources after inbound-reference replacement and pre-deletion validation. | The user explicitly started PR-04; deletion remains gated by zero-reference, broken-link, orphan, and control validation. |
| DOC-D003 | DECIDED | 2026-06-13 | PR-02 migrates the three canonical active protocols to English before legacy consolidation or full technical-document migration. | Creates a single English control language without mixing cleanup or broader translation scope. |
| ENG-D001 | DECIDED | 2026-06-13 | Adopt ESLint flat config with typed TypeScript linting, Prettier, `tsc --noEmit`, Ruff, and Pyright through sequential remediation PRs before enabling blocking CI gates. | PR-05 establishes audit-only foundations and baseline; no permanent legacy baseline, broad suppression, or mass-formatting shortcut is allowed. |
| AGENT-D001 | DECIDED | 2026-06-13 | Formalize seven Cursor operational identities; Codex is the separate control-plane orchestrator. | Removes the six-vs-seven ambiguity without merging Agent 1A and Agent 1B. |
| AGENT-D002 | DECIDED | 2026-06-13 | Agent 3 is an on-demand governance and architecture auditor. | Codex and the registries own routine state, locks, dependencies, and next-action control. |
| AGENT-D003 | DECIDED | 2026-06-13 | Trial Agent 2A/2B capability profiles under one permanent Agent 2 identity; do not split permanently yet. | Improves scope clarity while preserving one assignment slot and preventing unsafe same-identity parallel work. |
| UX30-D7 | DECIDED | 2026-06-13 | Implement **Phase 2B** (Suppliers + Categories) before Phase 2C (Price Lists). | User selected 2B; unblocks Plan Mode for `APP-PLATFORM-UX-3.0-PHASE-2B`. Phase 2C remains planned but not authorized until 2B completes. |
| UX30-D8 | DECIDED | 2026-06-13 | **Phase 2D-FULL** — full consolidation of shared list primitives across Products, Suppliers profiles, and Price Lists diff in one slice. | User selected 2D-FULL; unblocks Plan Mode for `APP-PLATFORM-UX-3.0-PHASE-2D`. Categories tree remains page-specific (not flat list). |
| CHROME-D31 | DECIDED | 2026-06-13 | Add **global command palette** (`Ctrl+K` / top-bar trigger) and **contextual route actions** (0–2 slots) to `AppTopBar`; first route target is catalog editor. | Explicit user approval; unblocks `APP-CHROME-3.1-TOPBAR-PALETTE-CONTEXT-ACTIONS`. |
| RUNTIME-D001 | DECIDED | 2026-06-13 | Formalize the RUNTIME-SYMMETRY program (PR-11 through PR-18) and rename shared control documents to platform-neutral names. | Enables Only Codex, Codex+Cursor, and Only Cursor symmetry without breaking authority paths. |
| RUNTIME-D002 | DECIDED | 2026-06-13 | Add explicit `Cursor Control Plane` identity; multi-chat is the default orchestration model for `ONLY_CURSOR`. | Separates control-plane runtimes from operational agents 1A–6. |
| RUNTIME-D003 | DECIDED | 2026-06-13 | Formalize runtime handoff procedure between Codex and Cursor Control Plane. | Enables safe control-plane continuity without state reinterpretation. |
| RUNTIME-D004 | DECIDED | 2026-06-13 | RUNTIME-SYMMETRY program (PR-11 through PR-18) validated and closed. | Project supports Only Codex, Codex+Cursor, and Only Cursor with symmetric protocols. |
| RUNTIME-D005 | DECIDED | 2026-06-13 | **Unified session mode** under `ONLY_CURSOR`: orchestration and implementation in the **same chat** when the user declares `Session: UNIFIED` and explicitly approves. | User may keep one chat for plan + execute; **no product-code edits** until the user approves the implementation slice (`APPROVE IMPLEMENTATION` or equivalent). Supplements multi-chat default from RUNTIME-D002; does not replace it. |
| RUNTIME-D006 | DECIDED | 2026-06-14 | **Task closure gate parity:** every runtime and `Session: UNIFIED` must execute the same post-QA closure sequence before `VALIDATED` — QA ACCEPT → `EVIDENCE_INDEX` → task packet → `TASK_REGISTRY` (status, lock release, queues) → `ORCHESTRATION_STATE` → `npm run control:validate`. | Prevents unified/single-chat sessions from skipping control-plane closure steps that multi-chat runtimes require. |
| ORDIA-D001 | DECIDED | 2026-06-14 | **Product name Ordia** for the portable orchestration and implementation control system (slug `ordia`, planned packages `@ordia/core`, `@ordia/cursor`, `@ordia/cli`). Narofitness is reference implementation v0, not the product owner. Domain guardrails and tracks remain project profile. | Unblocks extraction spec and future cross-project reuse; see `docs/archive/ordia/specs/SPEC_v0.1.md`. Product-level decisions use prefix `ORDIA-D###`. |
| ORDIA-D002 | DECIDED | 2026-06-14 | **Ordia manifest v0.2** — root `ordia.yaml` defines control paths, session capabilities, and enforcement roots; `scripts/ordia_config.py` loads it; hooks and `control:validate` consume the manifest. No control-document path migration in reference repo. | Unblocks portable configuration per project and `@ordia/core` extraction; see `docs/ordia/SPEC_v0.2.md`. |
| ORDIA-D003 | DECIDED | 2026-06-14 | **Ordia CLI v0.3** — `scripts/ordia_cli.py` with `init`, `validate`, `doctor`; greenfield templates under `docs/ordia/templates/minimal/`; npm scripts `ordia:init`, `ordia:validate`, `ordia:doctor`. | Enables new projects from zero without Narofitness registry; see `docs/archive/ordia/specs/SPEC_v0.3.md`. **Template path superseded for new work by ORDIA-D021** (`packages/ordia-core/ordia/templates/`). |
| ORDIA-D004 | DECIDED | 2026-06-14 | **Ordia core package v0.4** — `packages/ordia-core` owns manifest loader, CLI, and greenfield templates; `scripts/ordia_config.py` and `scripts/ordia_cli.py` are shims. | Completes roadmap phase 2; see `docs/archive/ordia/specs/SPEC_v0.4.md`. |
| ORDIA-D005 | DECIDED | 2026-06-14 | **Reference migration v0.4** — portable `ordia-*.mdc` Cursor rules replace legacy `narofitness-*.mdc` (except `narofitness-permanent-guardrails.mdc` project profile); hooks bootstrap via `packages/ordia-core`. | Completes roadmap phase 4; Narofitness control paths unchanged. |
| ORDIA-D006 | DECIDED | 2026-06-14 | **Ordia Cursor bundle v0.4** — `packages/ordia-cursor/templates/` ships hooks + rules; `ordia init --with-cursor` installs the bundle. | Completes roadmap phase 5 baseline; marketplace publish deferred. |
| ORDIA-D007 | DECIDED | 2026-06-14 | **Protocol path convention** — canonical location is `{control.root}/protocols/` (e.g. `docs/control/protocols/` greenfield, `docs/coordination/protocols/` if profile chooses). Product specs live under `docs/ordia/`; executable lifecycle protocols live under the control store. Narofitness reference **retains** existing flat `docs/coordination/*_PROTOCOL.md` as profile exception until an explicit profile migration task; greenfield and `@ordia/core` templates use `{control.root}/protocols/`. | Unblocks B-03; keeps manifest as single path authority for new projects. |
| ORDIA-D008 | DECIDED | 2026-06-14 | **Cursor hook command** — `ordia init --with-cursor` writes `hooks.json` using the **Python executable that ran init** (`sys.executable`), not a hardcoded `py -3` or `python3`. Bundle template uses a `{PYTHON}` placeholder replaced at install time. `ordia doctor` verifies each hook command is invocable. | Cross-platform without maintaining per-OS template trees; unblocks C-05. |
| ORDIA-D009 | DECIDED | 2026-06-14 | **Greenfield config runtime (layered)** — (1) **required:** stdlib inline manifest loader in `.cursor/hooks/lib/ordia_manifest.py` covering fields hooks need (`control`, `enforcement`); (2) **optional:** full `ordia-core` via `pip install -e`, vendored `vendor/ordia-core/`, or monorepo `packages/ordia-core/` for CLI/validate. Hooks must not depend on PyYAML or pip to classify paths. | Unblocks A-01; greenfield `--with-cursor` self-contained per audit P0. |
| ORDIA-D010 | DECIDED | 2026-06-14 | **Ordia product repo layout** — remain in **Narofitness monorepo** (`packages/ordia-core`, `packages/ordia-cursor`) through v0.5 exit gates and publish checklist. Extraction to a **separate `ordia` repository** is allowed only after v0.5 DONE + `PUBLISH_CHECKLIST` complete — not before. | Avoids premature split while reference implementation still co-evolves with Ordia; unblocks D-02 planning. |
| ORDIA-D011 | DECIDED | 2026-06-14 | **Decision ID namespaces** — **dual prefix:** product-level Ordia decisions use global `ORDIA-D###`; per-project decisions use **profile-scoped** prefixes (`CTRL-D`, `RUNTIME-D`, `UX30-D`, `{PROFILE}-D###`, etc.). Greenfield projects are not required to adopt `ORDIA-D###` for application/domain choices. | Closes SPEC v0.1 §10 decision-ID question; preserves Narofitness history. |
| ORDIA-D012 | DECIDED | 2026-06-14 | **Codex-only enforcement baseline** — without Cursor hooks, minimum viable enforcement is: (1) explicit `Runtime:` + `Protocol:` on change-capable prompts, (2) `ordia validate` / project validator in CI and before closure, (3) human approval gates unchanged. **Hook parity is not required** for Codex-only; optional Codex rule/skill pack may supplement later. Implementation details → `docs/ordia/CODEX_ENFORCEMENT_SPIKE.md` (Slice 5). | Closes SPEC v0.1 §10 Codex path; avoids blocking v0.5 on IDE hooks. |
| ORDIA-D013 | DECIDED | 2026-06-14 | **Greenfield distribution model** — **CLI-first:** `ordia init` scaffolds manifest + control store; `--with-cursor` installs hook/rule bundle. **Cursor marketplace extension is not required** for v0.5 GA — it is a post-v0.5 publish/distribution channel (`@ordia/cursor`), not a bootstrap dependency. | Closes SPEC v0.1 §10 extension vs CLI-only; aligns with ORDIA-D006 deferral of marketplace. |
| ORDIA-D014 | DECIDED | 2026-06-14 | **Closure validator subprocess** — when a task is `VALIDATED`, `validate_closure_gate` runs the command from `ordia.yaml` → `closure.validator` (default `npm run control:validate`) as a subprocess with timeout. Non-zero exit → **warning** by default; **`--strict-closure`** promotes to error. Does not replace the four existing structural checks (in_flight, packet, evidence, active state). | Closes audit gap G-CL01; aligns code with RUNTIME-D006 and SPEC closure claims; unblocks v0.6 A-03. |
| ORDIA-D015 | DECIDED | 2026-06-14 | **Command registry schema in Ordia core** — portable command catalog owned by `@ordia/core`: JSON Schema `commands.catalog.v1`, Python module `ordia/commands/`, fields matching reference `scripts/commands.catalog.json` (`meta`, `quickFlows`, `sections`, per-command metadata). Narofitness catalog is the reference profile instance, not the schema owner. | Unblocks v0.6 C-01; makes COMMANDS.md robustness exportable. |
| ORDIA-D016 | DECIDED | 2026-06-14 | **Manifest commands section (schema v0.3 additive)** — add optional `commands:` to `ordia.yaml`: `catalog` (path to catalog JSON), `npmPackage` (path to package.json), `validateOnControlCheck` (bool). **Additive only:** `version: "0.2"` manifests remain valid; loader accepts `"0.2"` and `"0.3"`. Separate `ordia.commands.yaml` rejected. | Unblocks v0.6 C-03 without breaking greenfield v0.2 scaffolds. |
| ORDIA-D017 | DECIDED | 2026-06-14 | **Session freshness on active-task change** — when `ORCHESTRATION_STATE` §0 `Active task ID` changes, hooks treat persisted session as stale for change-capable prompts until the user re-declares `Runtime:` + `Protocol:` or state re-seeds a valid pair. No TTL-based expiry in v0.6. | Unblocks v0.6 D-01; closes stale-session bypass without arbitrary timeouts. |
| ORDIA-D018 | DECIDED | 2026-06-14 | **Shell/git hook guard — spike only for v0.6** — do **not** implement `preToolUse` matchers for Shell/git in v0.6. Record findings in a spike doc; implementation requires a future explicit task after maintenance-cost review. Validator + prompt contract remain the enforcement backstop (`ORDIA-D012`). | Avoids scope creep; documents accepted IDE enforcement limits. |
| ORDIA-D019 | DECIDED | 2026-06-14 | **Spanish root product docs — translate to English, archive originals** — `docs/ANALISIS_FUNCIONAL.md` and `docs/ARQUITECTURA_TECNICA.md` (and any similar Spanish operational docs under `docs/`) are **translated to English** under `docs/product/` (canonical names: `FUNCTIONAL_ANALYSIS.md`, `TECHNICAL_ARCHITECTURE.md`). Originals move to `docs/archive/product/es/` with `Status: ARCHIVED` headers. **No Spanish operational technical docs** remain outside `docs/archive/` after v0.6 workstream E-3d. User-facing UI copy may remain Spanish per DOC-D001. | User mandate: all documentation in English; unblocks v0.6 E-3d. |
| ORDIA-D020 | DECIDED | 2026-06-14 | **Init optional full package docs** — `ordia init --with-docs` copies the full `packages/ordia-core/docs/` tree to target `docs/ordia/package/` (or equivalent). Default init copies only README + GREENFIELD excerpt links; does not bloat minimal scaffolds. | Unblocks v0.6 B-06 without forcing large doc trees on every greenfield. |
| ORDIA-D021 | DECIDED | 2026-06-14 | **Single template source for greenfield** — canonical scaffold templates live only under `packages/ordia-core/ordia/templates/`. Delete `docs/ordia/templates/`; update all references. Supersedes the path named in ORDIA-D003 for new work (historical decision row retained). | Closes audit gap G-A04; unblocks v0.6 A-01. |
| ORDIA-D022 | DECIDED | 2026-06-14 | **Model tier routing (v0.7)** — portable tiers **T0–T3** in `@ordia/core`; profile overlay in `MODEL_REGISTRY.yaml`; **recommend + user approve** (`APPROVE MODEL T*`) before change-capable work above T0; **`model_tier_min` enforced** (warn-only) against task registry + track minimums; Cursor hooks read `model` and log context via `preCompact`/`sessionEnd` to `temp/qa/model-usage/sessions.jsonl`; mismatch **warn-only** (no hard deny); **Cursor rate limit → Auto Mode only** (suppressed mismatch warnings); **Codex rate limit → no continuation** (switch runtime or defer); mandatory **Model usage** in every deliverable; Codex self-report only (`ORDIA-D012` unchanged). | Balances cost vs robustness; see `docs/ordia/MODEL_ROUTING_SPIKE.md`. |
| ORDIA-D023 | DECIDED | 2026-06-14 | **Workflow intents (v0.8)** — portable taxonomy of 18 core development intents; **`ordia workflow`** + **`ordia prompt emit/header`** generate standardized session headers, prompt bodies, and validation checklists; `workflowIntents[]` in command catalog; profile overlay for domain intents (IMPORT-FDL, page audit); warn-only intent validation in hooks; protocols remain normative authority — intents are entrypoints only. | Closes gap between shell commands and orchestration protocols; see `docs/ordia/WORKFLOW_INTENTS_SPIKE.md`. |

## Recording Rules

- Record a decision before implementation when alternatives have meaningful
  functional, architectural, commercial, or coordination consequences.
- Link decision IDs from `TASK_REGISTRY.yaml` and the relevant task packet.
- Do not silently reinterpret a `DECIDED` item.
- If revisiting a decision, create a new entry and mark the old one
  `SUPERSEDED` or `REVOKED`.
